<MotionScene xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ConstraintSet android:id="@+id/expanded">
        <Constraint
            android:id="@id/headerImage"
            android:layout_width="0dp"
            android:layout_height="240dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />
        <Constraint
            android:id="@id/headerTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="20sp"
            app:layout_constraintBottom_toBottomOf="@id/headerImage"
            app:layout_constraintStart_toStartOf="@id/headerImage"
            android:layout_margin="16dp" />
        <Constraint
            android:id="@id/recycler"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintTop_toBottomOf="@id/headerImage"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />
    </ConstraintSet>

    <ConstraintSet android:id="@+id/collapsed" app:deriveConstraintsFrom="@id/expanded">
        <Constraint
            android:id="@id/headerImage"
            android:layout_height="140dp" />
        <Constraint
            android:id="@id/headerTitle"
            android:textSize="18sp"
            app:layout_constraintBottom_toBottomOf="@id/headerImage"
            app:layout_constraintStart_toStartOf="@id/headerImage"
            android:layout_margin="12dp" />
    </ConstraintSet>

    <ConstraintSet android:id="@+id/stretched" app:deriveConstraintsFrom="@id/expanded">
        <Constraint
            android:id="@id/headerImage"
            android:layout_height="240dp" />
        <Constraint
            android:id="@id/headerTitle"
            android:textSize="22sp"
            app:layout_constraintBottom_toBottomOf="@id/headerImage"
            app:layout_constraintStart_toStartOf="@id/headerImage"
            android:layout_margin="18dp" />
    </ConstraintSet>

    <Transition
        android:id="@+id/transition_collapsed_expanded"
        app:constraintSetStart="@id/collapsed"
        app:constraintSetEnd="@id/expanded"
        app:duration="250" />

</MotionScene>

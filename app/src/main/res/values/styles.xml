<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 全局 MaterialButton 默认样式（统一圆角、颜色、高度、大小写等） -->
    <style name="Widget.StudyDemo.MaterialButton" parent="Widget.MaterialComponents.Button">
        <item name="android:minHeight">48dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">12dp</item>
        <item name="backgroundTint">?attr/colorPrimary</item>
        <item name="android:textColor">?attr/colorOnPrimary</item>
        <item name="rippleColor">?attr/colorPrimary</item>
        <item name="iconPadding">8dp</item>
    </style>

    <!-- 可选：描边（Outlined）按钮样式，若页面需要可通过 style="?attr/materialButtonOutlinedStyle" 使用 -->
    <style name="Widget.StudyDemo.MaterialButton.Outlined" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:minHeight">48dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">12dp</item>
        <item name="strokeColor">?attr/colorPrimary</item>
        <item name="strokeWidth">1dp</item>
        <item name="android:textColor">?attr/colorPrimary</item>
        <item name="backgroundTint">?attr/colorSurface</item>
    </style>

    <!-- 可选：文本按钮样式，若需要可单独使用（不默认接入） -->
    <style name="Widget.StudyDemo.MaterialButton.Text" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:minHeight">40dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">?attr/colorPrimary</item>
        <item name="backgroundTint">@android:color/transparent</item>
    </style>
</resources>